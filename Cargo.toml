[package]
name = "framework-desktop-fanledcontrol"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
nix = { version = "0.30", features = ["ioctl"] }
ctrlc = "3"
